services:
  - type: web
    name: badili-api
    rootDir: backend
    env: node
    buildCommand: apt-get update && apt-get install -y chromium-browser && PUPPETEER_SKIP_DOWNLOAD=true npm install && npm run build
    startCommand: npm start
    envVars:
      - key: PUPPETEER_EXECUTABLE_PATH
        value: /usr/bin/chromium-browser
      - key: PUPPETEER_SKIP_DOWNLOAD
        value: "true"
      - key: NODE_ENV
        value: production
